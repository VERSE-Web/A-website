const btn = document.getElementById("btnForAlert");

btn.addEventListener("click", function() {
    alert("This is not a fully working page yet. It is still under development, so some features may not work as expected.");
});
